import { Badge } from "@/components/ui/badge";
import { Panel } from "@/components/ui/panel";
import { agents } from "@/lib/demo-data";

export default function AgentsPage() { return <><header className="page-header"><div><p className="eyebrow">Specialist network</p><h1 className="page-title">Agent Control Center</h1><p className="page-subtitle">See which specialist agents are contributing to the current plan and how confident they are in their latest output.</p></div><Badge tone="green">8 AGENTS / READY</Badge></header><div className="agent-grid">{agents.map((agent) => <Panel key={agent.name} title={agent.name} detail={agent.role}><div className="agent-status"><Badge tone={agent.status === "Active" ? "green" : agent.status === "Review" ? "amber" : "slate"}>{agent.status}</Badge><strong>{agent.confidence}</strong></div><p className="agent-activity">{agent.activity}</p><div className="progress-track"><div className="progress-value" style={{ width: agent.confidence }} /></div><span className="metric-bottom">Latest activity · simulated</span></Panel>)}</div><p className="empty-note">Agent controls are read-only in this front-end shell. No model calls are active.</p></>; }
